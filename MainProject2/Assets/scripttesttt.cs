using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scripttesttt : MonoBehaviour
{
    // Start is called before the first frame update

    public Rigidbody b;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
